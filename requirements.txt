# Construction Volume Analysis - Python dependencies
# Install with:  pip install -r requirements.txt

# Web framework
Flask>=3.0

# HTTP client (WebODM API calls)
requests>=2.31

# AI / object detection (pulls in torch, torchvision, opencv, pillow, numpy)
ultralytics>=8.1

# Image processing (also pulled by ultralytics; pinned explicitly)
opencv-python>=4.9
numpy>=1.26

# Orthomosaic preview: GeoTIFF -> web JPG (imported directly by ortho_preview.py)
Pillow>=10.0

# Reporting - Excel export (Phase 6)
openpyxl>=3.1

# Charts - S-curve generation (Phase 6)
matplotlib>=3.8

# Geo-aware DEM volume math (required - the primary volume method + Z-offset
# correction live here). Installs cleanly on Windows via pip wheels.
rasterio>=1.3

# Upload progress for the WebODM submit helper scripts
requests-toolbelt>=1.0
